# 測試與正式環境必須使用不同網址與不同配對碼。
SYNC_TEST_DOMAIN=sync-test.example.com
SYNC_PROD_DOMAIN=sync.example.com

# 請產生至少 16 字元的隨機值，僅在要配對裝置時使用。
TEST_PAIR_CODE=replace-with-a-long-test-pairing-secret
PROD_PAIR_CODE=replace-with-a-long-production-pairing-secret
