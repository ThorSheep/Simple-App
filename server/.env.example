# A public DNS name pointing at this server, for example sync.example.com.
SYNC_DOMAIN=sync.example.com

# Generate a random value of at least 16 characters. Use it only while pairing a device.
PAIR_CODE=replace-with-a-long-random-secret
